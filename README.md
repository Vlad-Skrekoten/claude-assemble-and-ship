# QA Kit Plugin

This plugin provides utilities for code review and branch change summarization.

## Features
- **Commands**: `/qa-kit:summarize-changes` — Summarizes changes on the current branch.
- **Agents**: `code-reviewer` — Subagent that automatically reviews recent code changes.

## Usage
Load the plugin in Claude Code using:
`claude --plugin-dir .`
